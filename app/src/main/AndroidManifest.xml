<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smart_chlothing_app">

    <application
        android:icon="@mipmap/calibrate_logo1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/calibrate_logo1_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Smart_Chlothing_App">
        <activity android:name=".FeelingActivity"></activity>
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>